<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{product.name}} - My Product Site</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>

    <main class="container my-5">
        <div class="row g-5 align-items-center">
            <!-- Cột hình ảnh sản phẩm -->
            <div class="col-md-6">
                <div class="product-image-wrapper"> <!-- Khung chứa ảnh với viền bo góc -->
                    <img src="{{product.image}}" class="img-fluid" alt="{{product.name}}">
                </div>
            </div>

            <!-- Cột chi tiết sản phẩm -->
            <div class="col-md-6 product-details">
                <h2 class="display-5">{{product.name}}</h2>
                <p class="lead">{{product.description}}</p>
                <p class="fs-4 text-primary"><strong>Price:</strong> {{product.price}}</p>
                <a href="/" class="btn btn-custom mt-4">Back to Home</a>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
